# Historical Test Cases

1. [ ] Open a file dialog (e.g. Click "Open" in "Options" window), check:
  - [ ] Focus the main window works.
  - [ ] Click "close" button of the main window actually closes the main window (instead of locking up or crash), but keeps the file dialog and the process alive.
  - [ ] Click "close" button of the file dialog closes the file dialog. Even after closing the main window.
  - [ ] The process exits only if there are no visible windows.

